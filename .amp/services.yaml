services:
  slp-preview:
    command: >-
      AMP_PORTAL_PREVIEW=1 npm run build:web &&
      npm run preview -- --host 0.0.0.0 --port "$PORT"
    health: /
    portal:
      url: /
      title: SLP Preview
      description: Bundled preview optimized for orb portal latency.
